// Generated by CoffeeScript 1.7.1
var FibaroServer;

FibaroServer = (function() {
  function FibaroServer(serverURL) {
    this.serverURL = serverURL;
    this.ROOMS_URL = this.serverURL + "/api/rooms";
    this.SCENES_URL = this.serverURL + "/api/scenes";
  }

  FibaroServer.prototype.setReadyCallback = function(dataReadyCallback) {
    this.dataReadyCallback = dataReadyCallback;
  };

  FibaroServer.prototype.getActionGroups = function() {
    return $.ajax(this.ROOMS_URL, {
      type: "GET",
      dataType: "json",
      crossDomain: true,
      error: (function(_this) {
        return function(jqXHR, textStatus, errorThrown) {
          return alert("Fibaro Rooms failed: " + textStatus + " " + errorThrown);
        };
      })(this),
      success: (function(_this) {
        return function(data, textStatus, jqXHR) {
          alert("Fibaro done ok");
          _this.rooms = data;
          return $.ajax(_this.SCENES_URL, {
            type: "GET",
            dataType: "json",
            crossDomain: true,
            success: function(data, textStatus, jqXHR) {
              _this.scenes = _this.getScenes(data, _this.rooms);
              _this.scenes.sort(_this.sortByRoomName);
              return _this.dataReadyCallback(_this.scenes);
            }
          });
        };
      })(this)
    });
  };

  FibaroServer.prototype.makeBasicAuth = function(username, password) {
    var hash, tok;
    tok = username + ':' + password;
    hash = btoa(tok);
    return 'Basic ' + hash;
  };

  FibaroServer.prototype.sortByRoomName = function(a, b) {
    if (a.groupName < b.groupName) {
      return -1;
    }
    if (a.groupName > b.groupName) {
      return 1;
    }
    return 0;
  };

  FibaroServer.prototype.getScenes = function(data, rooms) {
    var fibaroScene, fibaroScenes, room, scene, sceneCmd, scenes, _i, _j, _len, _len1;
    fibaroScenes = data;
    scenes = [];
    for (_i = 0, _len = fibaroScenes.length; _i < _len; _i++) {
      fibaroScene = fibaroScenes[_i];
      if (!"roomID" in fibaroScene) {
        continue;
      }
      for (_j = 0, _len1 = rooms.length; _j < _len1; _j++) {
        room = rooms[_j];
        if (!(("id" in room) && ("name" in room))) {
          continue;
        }
        if (room.id === fibaroScene.roomID) {
          sceneCmd = this.serverURL + "/api/sceneControl?id=" + fibaroScene.id + "&action=start";
          scene = {
            actionNum: fibaroScene.id,
            actionName: fibaroScene.name,
            groupName: room.name,
            actionUrl: sceneCmd
          };
          scenes.push(scene);
          break;
        }
      }
    }
    return scenes;
  };

  return FibaroServer;

})();
