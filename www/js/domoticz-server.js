// Generated by CoffeeScript 1.12.7
(function() {
  App.DomoticzServer = (function() {
    function DomoticzServer(manager, serverDef, dataReadyCallback) {
      this.manager = manager;
      this.serverDef = serverDef;
      this.dataReadyCallback = dataReadyCallback;
      this.ACTIONS_URI = this.serverDef.url + "/json.htm?type=scenes";
      this.numRefreshFailuresSinceSuccess = 0;
      this.firstRefreshAfterFailSecs = 10;
      this.nextRefreshesAfterFailSecs = 60;
      return;
    }

    DomoticzServer.prototype.setReadyCallback = function(dataReadyCallback) {
      this.dataReadyCallback = dataReadyCallback;
    };

    DomoticzServer.prototype.reqActions = function() {
      console.log("WallTabletDebug Requesting Domoticz data from " + this.ACTIONS_URI);
      $.ajax(this.ACTIONS_URI, {
        type: "GET",
        dataType: "json",
        crossDomain: true,
        success: (function(_this) {
          return function(data, textStatus, jqXHR) {
            _this.processRecvdActions(data);
            console.log("WallTabletDebug Received Domoticz data from " + _this.ACTIONS_URI);
            _this.numRefreshFailuresSinceSuccess = 0;
            App.LocalStorage.set(_this.ACTIONS_URI, data);
          };
        })(this),
        error: (function(_this) {
          return function(jqXHR, textStatus, errorThrown) {
            var storedData;
            console.log("WallTabletDebug " + " DOMOTICZ AjaxFail Status= " + textStatus + " URL= " + _this.ACTIONS_URI + " Error= " + errorThrown);
            App.LocalStorage.logEvent("DomLog", "AjaxFail Status= " + textStatus + " URL= " + _this.ACTIONS_URI + " Error= " + errorThrown);
            _this.numRefreshFailuresSinceSuccess++;
            setTimeout(function() {
              return _this.getActionGroups;
            }, (_this.numRefreshFailuresSinceSuccess === 1 ? _this.firstRefreshAfterFailSecs * 1000 : _this.nextRefreshesAfterFailSecs * 1000));
            storedData = App.LocalStorage.get(_this.ACTIONS_URI);
            console.log("WallTabletDebug Getting data stored for " + _this.ACTIONS_URI + " result = " + storedData);
            if (storedData != null) {
              console.log("WallTabletDebug Using stored data for " + _this.ACTIONS_URI);
              _this.processRecvdActions(storedData);
            }
          };
        })(this)
      });
    };

    DomoticzServer.prototype.processRecvdActions = function(data) {
      this.scenes = this.getScenes(data);
      this.scenes.sort(this.sortByRoomName);
      return this.dataReadyCallback(this.serverDef.name, this.scenes);
    };

    DomoticzServer.prototype.sortByRoomName = function(a, b) {
      if (a.groupName < b.groupName) {
        return -1;
      }
      if (a.groupName > b.groupName) {
        return 1;
      }
      return 0;
    };

    DomoticzServer.prototype.getScenes = function(data, rooms) {
      var actionName, i, len, name, roomName, scene, scenes, serverScene, serverScenes;
      serverScenes = data.result;
      scenes = [];
      if (serverScenes != null) {
        for (i = 0, len = serverScenes.length; i < len; i++) {
          serverScene = serverScenes[i];
          name = serverScene.Name.split(" - ");
          actionName = serverScene.Name;
          if (name.length > 1) {
            roomName = name[0].trim();
            actionName = name[1].trim();
          }
          scene = {
            actionNum: "",
            actionName: roomName + " " + actionName,
            groupName: roomName,
            actionUrl: this.serverDef.url + "/json.htm?type=command&param=switchscene&idx=" + serverScene.idx + "&switchcmd=On",
            iconName: this.manager.getIconFromActionName(actionName, this.serverDef.iconAliasing)
          };
          scenes.push(scene);
        }
      }
      return scenes;
    };

    return DomoticzServer;

  })();

}).call(this);

//# sourceMappingURL=domoticz-server.js.map
